<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('navbar') %>
    <div class="row vh-100">
        <div class="col-4">
            <%- include('functionlist-m') %>
        </div>
        <div class="col-8 d-flex align-items-center">
            <div class="container text-center">
                <h1>優惠券設定設定</h1>
                <form method="post" accept-charset="utf-8">
                    <title>優惠券設定</title>
                    <div class="mb-2">不續訂顧客:</div>
                    <div class="mb-2">
                        <label>一個月以內:</label>
                        <select name="month" id="s1" class="form-control d-inline" style="width: 110px;">
                            <option value="1">A優惠券</option>
                        </select>
                    </div>
                    <div class="mb-2">
                        <label>一月至一季:</label>
                        <select name="season" id="s2" class="form-control d-inline" style="width: 110px;">
                            <option value="1">A優惠券</option>
                            <option value="2">B優惠券</option>
                        </select>
                    </div>
                    <div class="mb-2">
                        <label>一季至半年:</label>
                        <select name="half" id="s3" class="form-control d-inline" style="width: 110px;">
                            <option value="1">A優惠券</option>
                            <option value="2">B優惠券</option>
                            <option value="3">C優惠券</option>
                        </select>
                    </div>
                    <div class="mb-2">
                        <label>半年至一年:</label>
                        <select name="year" id="s4" class="form-control d-inline" style="width: 110px;">
                            <option value="1">A優惠券</option>
                            <option value="2">B優惠券</option>
                            <option value="3">C優惠券</option>
                            <option value="4">D優惠券</option>
                        </select>
                    </div>
                    <input type='submit' value='儲存' class="btn btn-primary">
                </form>
            </div>
        </div>
    </div>
</body>
<script src="https://code.jquery.com/jquery-3.5.1.min.js"
    integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
<script>
    //get data in pageController
    let data = '<%-JSON.stringify(data)%>'
    data = JSON.parse(data)
    $("#s2").children().each(function () {
        if ($(this).val() == data.season_coupon_id) {
            $(this).prop("selected", true); //或是給"selected"也可
        }
    });
    $("#s3").children().each(function () {
            if ($(this).val() == data.half_coupon_id) {
                $(this).prop("selected", true); //或是給"selected"也可
            }
    });
    $("#s4").children().each(function () {
            if ($(this).val() == data.year_coupon_id) {
                $(this).prop("selected", true); //或是給"selected"也可
            }
    });
</script>
</html>